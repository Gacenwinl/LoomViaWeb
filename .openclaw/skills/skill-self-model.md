# Skill: Self-Model 自动更新（草案 · Unreleased）

## 目标

从 REVIEW / OKR 完成情况增量更新 `.openclaw/MEMORY.md` 中「用户工作特征」与 KR 进度摘要。

## 触发（规划）

- 每周日 22:00 或用户「更新自我模型」

## 约束

- 单次变更 ≤ 20 行
- 必须保留「已知陷阱」章节
- 变更前 `read` 旧版，变更后写入 `MEMORY.md.backup.YYYYMMDD`

## 状态

**未在生产启用** — 见 `docs/roadmap-self-model.md`
